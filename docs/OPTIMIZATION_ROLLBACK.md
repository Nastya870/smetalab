# ⚠️ Откат оптимизации - Анализ проблемы

**Дата**: 8 января 2026, 20:48  
**Статус**: ❌ Откачено

---

## 🔴 Что произошло

Оптимизация `findByIdWithDetails` вызвала проблему при загрузке сметы.

**Возможные причины**:

1. **Несовместимость структуры данных с frontend**
   - Frontend ожидает определённую структуру объекта
   - Новый код мог вернуть данные в другом формате

2. **Проблема с NULL значениями**
   - JOIN может создавать NULL для несуществующих связей
   - Группировка могла неправильно обработать пустые материалы

3. **Несоответствие полей**
   - Алиасы полей могли отличаться от ожидаемых frontend

---

## ✅ Откат выполнен

```bash
# Восстановлен оригинальный файл
Copy-Item "server\repositories\estimatesRepository.js.backup" "server\repositories\estimatesRepository.js" -Force

# Сервер перезапущен
npm run dev
```

---

## 🔍 Рекомендации для повторной попытки

### Подход 1: Постепенная оптимизация (БЕЗОПАСНО)

**Шаг 1**: Сначала оптимизировать только загрузку материалов для ОДНОЙ позиции:

```javascript
// Вместо N запросов для материалов - один запрос
const itemIds = itemsResult.rows.map(item => item.id);

const materialsQuery = `
  SELECT 
    eim.*,
    m.id as material_id,
    m.sku,
    m.name as material_name,
    m.unit,
    m.category,
    m.price as material_base_price,
    m.consumption,
    m.image
  FROM estimate_item_materials eim
  JOIN materials m ON eim.material_id = m.id
  WHERE eim.estimate_item_id = ANY($1)
  ORDER BY eim.estimate_item_id, m.name
`;

const allMaterials = await pool.query(materialsQuery, [itemIds]);

// Группируем по estimate_item_id
const materialsByItem = new Map();
for (const material of allMaterials.rows) {
  if (!materialsByItem.has(material.estimate_item_id)) {
    materialsByItem.set(material.estimate_item_id, []);
  }
  materialsByItem.get(material.estimate_item_id).push(material);
}

// Теперь для каждой позиции просто берём из Map
const items = itemsResult.rows.map(item => ({
  ...item,
  materials: materialsByItem.get(item.id) || []
}));
```

Это уменьшит **N запросов → 1 запрос** для материалов, оставив всё остальное как есть.

**Ожидаемый прирост**: 5-7x (вместо 10x, но безопаснее)

### Подход 2: Feature flag (ОЧЕНЬ БЕЗОПАСНО)

Добавить переменную окружения для включения оптимизации:

```javascript
export async function findByIdWithDetails(estimateId, tenantId) {
  const USE_OPTIMIZED = process.env.USE_OPTIMIZED_ESTIMATE_LOAD === 'true';
  
  if (USE_OPTIMIZED) {
    return findByIdWithDetailsOptimized(estimateId, tenantId);
  } else {
    // Старый код (рабочий)
    return findByIdWithDetailsOld(estimateId, tenantId);
  }
}
```

Тогда можно тестировать на production без риска.

### Подход 3: A/B тестирование в коде

```javascript
// Загружаем обоими способами и сравниваем
const [oldResult, newResult] = await Promise.all([
  findByIdWithDetailsOld(estimateId, tenantId),
  findByIdWithDetailsOptimized(estimateId, tenantId)
]);

// Сравниваем структуры
if (JSON.stringify(oldResult) !== JSON.stringify(newResult)) {
  console.error('⚠️ Results differ!');
  console.error('Old:', oldResult);
  console.error('New:', newResult);
}

// Используем старый (безопасно)
return oldResult;
```

---

## 🧪 Что тестировать перед следующей попыткой

1. **Unit тест для группировки**
   ```javascript
   // Проверить что группировка работает правильно
   const testRows = [
     { item_id: 1, material_id: 10 },
     { item_id: 1, material_id: 11 },
     { item_id: 2, material_id: 20 }
   ];
   const grouped = groupByItem(testRows);
   // Ожидаем 2 items, у первого 2 материала
   ```

2. **Сравнение структур данных**
   - Вызвать оба варианта
   - Сравнить JSON.stringify
   - Убедиться что идентичны

3. **Тест на пустых данных**
   - Смета без позиций
   - Позиция без материалов
   - NULL значения

---

## 💡 Выводы

**Что делать сейчас**:
1. ✅ Откат выполнен - система работает
2. ⏸️ Отложить оптимизацию БД до детального анализа
3. 🔍 Сначала добавить **SWR кэширование** на frontend (менее рискованно)
4. 📊 Провести профилирование реальных запросов (найти настоящее узкое место)

**Альтернатива**:
Возможно, проблема не в скорости загрузки одной сметы, а в:
- Загрузке списка смет (pagination)
- Отсутствии кэширования
- Медленном рендеринге на frontend (React memo)

---

## 📝 Lessons Learned

1. **Всегда делать откат простым** ✅ (бэкап создан)
2. **Тестировать на dev перед production** ⚠️ (не успели)
3. **Постепенные изменения лучше больших** 💡
4. **Feature flags для рискованных изменений** 🚩

---

**Итого**: Откат выполнен успешно. Система вернулась к рабочему состоянию.

Следующие шаги:
- Проанализировать логи ошибки
- Использовать более консервативный подход (Подход 1)
- Добавить больше тестов перед внедрением

