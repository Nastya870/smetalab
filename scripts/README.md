# Скрипты управления пользователями и ролями

## Доступные команды

### 1. Просмотр пользователей и ролей

```bash
npm run db:users
```

Выводит список всех пользователей с информацией:
- Email и статус верификации
- Компания (tenant)
- Назначенная роль
- Дата регистрации
- Статистику по ролям

### 2. Назначение роли super_admin

```bash
npm run db:set-admin <email>
```

Пример:
```bash
npm run db:set-admin kiy026@yandex.ru
```

Назначает пользователю роль **super_admin** (полный доступ ко всему).

**⚠️ ВАЖНО:** После изменения роли пользователь должен выйти и войти заново!

---

## Роли в системе

### 👑 super_admin (Супер Администратор)
- Полный доступ ко всем функциям
- Управление пользователями, ролями и разрешениями
- Видит все пункты меню независимо от настроек
- Не может быть ограничен через систему разрешений

### 🔐 admin (Администратор)
- Управление пользователями компании
- Управление проектами, сметами, закупками
- Доступ к справочникам
- Просмотр отчетов
- Не может управлять super_admin пользователями

### 📊 manager (Менеджер проектов)
- Создание и редактирование проектов и смет
- Управление закупками
- Просмотр справочников
- Ограниченный доступ к отчетам

### 👤 worker (Сметчик)
- Просмотр проектов и смет
- Только чтение справочников
- Минимальные права

---

## Примеры использования

### Сделать пользователя super_admin

```bash
# 1. Посмотреть список пользователей
npm run db:users

# 2. Назначить роль super_admin
npm run db:set-admin user@example.com

# 3. Проверить изменения
npm run db:users
```

### Массовое назначение ролей

Если нужно назначить роль нескольким пользователям, используйте скрипт напрямую:

```bash
node scripts/set-super-admin.mjs user1@example.com
node scripts/set-super-admin.mjs user2@example.com
node scripts/set-super-admin.mjs user3@example.com
```

---

## Технические детали

### Структура БД

```sql
-- Таблица пользователей
users (id, email, full_name, status, email_verified)

-- Связь пользователь-компания
user_tenants (user_id, tenant_id, is_default)

-- Роли
roles (id, key, name)

-- Назначение ролей
user_role_assignments (user_id, tenant_id, role_id)
```

### Логика работы

1. **Один пользователь** может быть в **нескольких компаниях**
2. **В каждой компании** пользователь может иметь **разные роли**
3. **is_default = true** определяет компанию по умолчанию при входе
4. **super_admin** - глобальная роль, работает во всех компаниях

---

## Troubleshooting

### Проблема: Пользователь не видит меню после назначения роли

**Решение:**
1. Выйдите из системы (Logout)
2. Очистите кэш браузера (Ctrl+Shift+Del)
3. Войдите заново (Login)

### Проблема: Ошибка "User not found"

**Решение:**
- Проверьте правильность email (регистр НЕ важен)
- Убедитесь, что пользователь зарегистрирован через `/auth/register`

### Проблема: Ошибка подключения к БД

**Решение:**
- Проверьте `.env` файл и переменную `DATABASE_URL`
- Убедитесь, что БД доступна (Render PostgreSQL)
- Проверьте соединение через `psql $DATABASE_URL`

---

## Безопасность

⚠️ **ВАЖНО:**

1. **Не давайте роль super_admin** всем подряд - это полный доступ!
2. **Регулярно проверяйте** список super_admin через `npm run db:users`
3. **В production** ограничьте доступ к этим скриптам
4. **Backend всегда проверяет** роли независимо от frontend

---

## См. также

- [PERMISSIONS_SYSTEM.md](./PERMISSIONS_SYSTEM.md) - Документация по системе разрешений
- [DATABASE_STATUS.md](./database/DATABASE_STATUS.md) - Статус базы данных
