# 🗑️ Удаление поля "Категория" из справочника работ

**Дата изменений:** 17 ноября 2025 г.  
**Версия:** v1.17.2  
**Причина:** Поле "Категория" не используется, вместо него применяется иерархия: Фаза → Раздел → Подраздел

---

## 🎯 Суть изменений

В справочнике работ удалено поле **"Категория"** (`category`), так как:

1. ❌ Не отображается в форме добавления/редактирования работы (`WorkDialog.jsx`)
2. ❌ Дублирует функционал иерархии (Фаза, Раздел, Подраздел)
3. ✅ Иерархия более гибкая и информативная
4. ✅ Соответствует реальной структуре строительных работ

---

## 📦 Что изменилось

### ❌ Было (с категорией):

**Структура CSV:**
```csv
Код;Наименование;Категория;Ед изм;Базовая цена;Фаза;Раздел;Подраздел
01-001;Разработка грунта;Земляные работы;м³;450;Подготовительные работы;Земляные работы;Разработка грунта
```

**Форма добавления работы:**
- Код ✓
- Наименование ✓
- **Категория** ❌ (не было в форме!)
- Ед. изм. ✓
- Базовая цена ✓
- Фаза → Раздел → Подраздел ✓

---

### ✅ Стало (без категории):

**Структура CSV:**
```csv
Код;Наименование;Ед изм;Базовая цена;Фаза;Раздел;Подраздел
01-001;Разработка грунта;м³;450;Подготовительные работы;Земляные работы;Разработка грунта
```

**Форма добавления работы:**
- Код ✓
- Наименование ✓
- Ед. изм. ✓
- Базовая цена ✓
- Фаза → Раздел → Подраздел ✓

**Теперь CSV полностью соответствует форме!**

---

## 🔧 Технические изменения

### 1. Шаблон импорта CSV

**Файл:** `shared/lib/api/worksImportExport.js`

**Функция:** `downloadTemplate()`

**Изменения:**
- ❌ Удалена колонка `'Категория'` из всех примеров
- ✅ Осталось 7 колонок вместо 8

---

### 2. Экспорт работ в CSV

**Файл:** `shared/lib/api/worksImportExport.js`

**Функция:** `exportWorks()`

**Изменения:**
```javascript
// ❌ Было:
const exportData = works.map(work => ({
  'Код': work.code,
  'Наименование': work.name,
  'Категория': work.category || '',  // ← Удалено
  'Ед изм': work.unit || 'шт',
  // ...
}));

// ✅ Стало:
const exportData = works.map(work => ({
  'Код': work.code,
  'Наименование': work.name,
  'Ед изм': work.unit || 'шт',
  // ...
}));
```

---

### 3. Импорт работ из CSV

**Файл:** `app/references/works/ImportDialog.jsx`

**Изменения:**
```javascript
// ❌ Было:
works.push({
  code: code?.trim(),
  name: name?.trim(),
  category: (row['Категория'] || row['category'] || '')?.trim(),  // ← Удалено
  unit: (row['Ед изм'] || row['Ед. изм.'] || row['unit'] || 'шт')?.trim(),
  // ...
});

// ✅ Стало:
works.push({
  code: code?.trim(),
  name: name?.trim(),
  unit: (row['Ед изм'] || row['Ед. изм.'] || row['unit'] || 'шт')?.trim(),
  // ...
});
```

**Обновлено описание:**
```
Дополнительные поля: Ед изм, Базовая цена, Фаза, Раздел, Подраздел
```

---

### 4. Создание работы

**Файл:** `app/references/works/index.jsx`

**Функция:** `handleSave()` (создание)

**Изменения:**
```javascript
// ❌ Было:
const created = await worksAPI.create({
  code: currentWork.code,
  name: currentWork.name,
  category: currentWork.category,  // ← Удалено
  unit: currentWork.unit,
  // ...
});

// ✅ Стало:
const created = await worksAPI.create({
  code: currentWork.code,
  name: currentWork.name,
  unit: currentWork.unit,
  // ...
});
```

---

### 5. Обновление работы

**Файл:** `app/references/works/index.jsx`

**Функция:** `handleSave()` (редактирование)

**Изменения:**
```javascript
// ❌ Было:
const updated = await worksAPI.update(currentWork.id, {
  code: currentWork.code,
  name: currentWork.name,
  category: currentWork.category,  // ← Удалено
  unit: currentWork.unit,
  // ...
});

// ✅ Стало:
const updated = await worksAPI.update(currentWork.id, {
  code: currentWork.code,
  name: currentWork.name,
  unit: currentWork.unit,
  // ...
});
```

---

### 6. Полнотекстовый поиск

**Файл:** `app/references/works/index.jsx`

**Изменения:**
```javascript
// ❌ Было:
return fullTextSearch(works, searchTerm, ['name', 'code', 'unit', 'category']);

// ✅ Стало (добавлены поля иерархии):
return fullTextSearch(works, searchTerm, ['name', 'code', 'unit', 'phase', 'section', 'subsection']);
```

**Теперь поиск работает по:**
- Наименованию
- Коду
- Единице измерения
- **Фазе**
- **Разделу**
- **Подразделу**

---

### 7. Исправление импортов в WorkDialog

**Файл:** `app/references/works/WorkDialog.jsx`

**Добавлены недостающие импорты:**
```javascript
import {
  // ...
  useTheme,        // ← Добавлено
  useMediaQuery    // ← Добавлено
} from '@mui/material';
```

---

## 🗄️ База данных

### ⚠️ Важно: Поле category остаётся в БД!

В таблице `works` поле `category` **НЕ удаляется** из базы данных:

```sql
CREATE TABLE works (
  id SERIAL PRIMARY KEY,
  code VARCHAR(50) NOT NULL UNIQUE,
  name VARCHAR(255) NOT NULL,
  category VARCHAR(100) NOT NULL,  -- ← Остаётся в БД
  unit VARCHAR(50) NOT NULL,
  base_price DECIMAL(10, 2) NOT NULL DEFAULT 0.00,
  phase VARCHAR(100),              -- Иерархия
  section VARCHAR(100),
  subsection VARCHAR(100),
  -- ...
);
```

**Причины:**
1. 🔒 **Обратная совместимость** - старые данные не ломаются
2. 📊 **Миграция** - можно позже перенести данные из category в иерархию
3. ⚡ **Безопасность** - не нужно менять схему БД в production

**Что происходит с category:**
- ❌ Не используется в UI
- ❌ Не отображается в формах
- ❌ Не экспортируется/импортируется
- ✅ Просто игнорируется

---

## 📝 Примеры использования

### Заполнение CSV файла (новый формат):

| Код | Наименование | Ед изм | Базовая цена | Фаза | Раздел | Подраздел |
|-----|--------------|--------|--------------|------|---------|-----------|
| 01-001 | Разработка грунта экскаватором | м³ | 450 | Подготовительные работы | Земляные работы | Разработка грунта |
| 02-001 | Устройство бетонной подготовки | м³ | 3200 | Основные строительные работы | Бетонные работы | Бетонная подготовка |
| 03-001 | Кладка стен из кирпича | м³ | 4500 | Основные строительные работы | Каменные работы | Кладка стен |

### В Excel это выглядит так:

```
Код       | Наименование                      | Ед изм | Базовая цена | Фаза                              | Раздел           | Подраздел
01-001    | Разработка грунта экскаватором   | м³     | 450          | Подготовительные работы          | Земляные работы  | Разработка грунта
02-001    | Устройство бетонной подготовки   | м³     | 3200         | Основные строительные работы     | Бетонные работы  | Бетонная подготовка
03-001    | Кладка стен из кирпича           | м³     | 4500         | Основные строительные работы     | Каменные работы  | Кладка стен
```

---

## 🧪 Как протестировать

### 1. Скачать новый шаблон

1. Откройте http://localhost:3000/app/references/works
2. Нажмите **"Импорт"**
3. Нажмите **"Скачать шаблон CSV"**
4. Откройте файл в Excel

**Ожидаемый результат:**
- ✅ 7 колонок (без "Категория")
- ✅ Колонки: Код, Наименование, Ед изм, Базовая цена, Фаза, Раздел, Подраздел
- ✅ Точка с запятой как разделитель
- ✅ Нет кавычек

---

### 2. Импортировать работы

1. Заполните шаблон в Excel
2. Сохраните как **CSV UTF-8**
3. Загрузите через диалог импорта
4. Убедитесь, что работы добавились

**Проверка:**
- ✅ Работы импортировались
- ✅ Иерархия (Фаза, Раздел, Подраздел) заполнена
- ✅ Нет ошибок

---

### 3. Проверить поиск

1. Откройте справочник работ
2. Введите в поиск название фазы (например, "Подготовительные")
3. Убедитесь, что находятся работы с этой фазой

**Проверка:**
- ✅ Поиск работает по фазе
- ✅ Поиск работает по разделу
- ✅ Поиск работает по подразделу

---

### 4. Создать работу вручную

1. Нажмите **"Добавить работу"**
2. Заполните форму
3. Выберите **Фазу → Раздел → Подраздел** через Autocomplete
4. Сохраните

**Проверка:**
- ✅ Форма не содержит поле "Категория"
- ✅ Иерархия работает через Autocomplete
- ✅ Работа создаётся без ошибок

---

## 🔄 Миграция данных (опционально)

Если в БД есть старые работы с заполненной `category`, можно перенести данные в иерархию:

```sql
-- Пример миграции: category → phase
UPDATE works 
SET phase = category 
WHERE phase IS NULL 
  AND category IS NOT NULL;
```

**Но это не обязательно!** Можно просто оставить category в БД и игнорировать её.

---

## 📋 Связанные файлы

**Frontend:**
- ✅ `shared/lib/api/worksImportExport.js` - шаблон и экспорт CSV
- ✅ `app/references/works/ImportDialog.jsx` - импорт CSV
- ✅ `app/references/works/index.jsx` - создание, обновление, поиск
- ✅ `app/references/works/WorkDialog.jsx` - форма работы (исправлены импорты)
- ✅ `app/references/works/mockData.js` - уже без category

**Backend:**
- ⚠️ Изменения не требуются (category просто игнорируется на фронте)

**Database:**
- ⚠️ Миграция не требуется (поле остаётся в таблице)

---

## ✅ Результаты

### Преимущества:

✅ **Упрощение:** Меньше полей для заполнения  
✅ **Единообразие:** CSV соответствует форме  
✅ **Иерархия:** Используется правильная структура (Фаза → Раздел → Подраздел)  
✅ **Поиск:** Расширен на иерархию  
✅ **Обратная совместимость:** Старые данные не ломаются  

### Что осталось:

- ⚠️ Поле `category` в БД (для совместимости)
- ✅ Всё остальное работает без category

---

**Готово!** Поле "Категория" успешно удалено из интерфейса и CSV! 🎉
