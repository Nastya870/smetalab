# Миграции базы данных - SmetaLab

Эта директория содержит актуальные миграции для базы данных SmetaLab.

## 🚀 Как это работает

Мы используем систему **Baseline + Incremental migrations**:

1. **Baseline (`001_complete_schema.sql`)**:
   - Содержит полную схему базы данных (все таблицы, индексы, RLS политики, функции).
   - Применяется автоматически только если база данных **пустая**.
   - Актуален на момент миграции **071** (включая иерархические категории).

2. **Incremental Migrations (072+)**:
   - Новые изменения, которые применяются поверх существующей базы.
   - Каждая миграция регистрируется в таблице `schema_version`.

## 🛠️ Запуск миграций

Для запуска миграций используйте скрипт:
```bash
node scripts/runMigrations.js
```

Скрипт сам определит, нужно ли накатывать Baseline или только новые изменения.

---
**Важно**: Никогда не удаляйте `001_complete_schema.sql`, так как он является основой для новых деплоев.
