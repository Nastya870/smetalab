# 🚀 План оптимизации Smeta Pro

**Дата**: 20 декабря 2025  
**Цель**: Улучшить производительность справочников и смет

---

## 📊 Анализ текущего состояния

### Проблемы:

#### 1. **Справочники (Materials & Works)**
- ❌ Загрузка всех 50,000 материалов за раз (~5-10 MB)
- ❌ Время первой загрузки: 3-5 секунд
- ❌ Повторная загрузка на каждой странице
- ❌ Нагрузка на Free tier Render (512 MB RAM)
- ✅ Есть кэширование на backend (NodeCache, TTL 1 час)
- ✅ Есть виртуализация (react-window) на frontend

#### 2. **Сметы (Estimates)**
- ❌ N+1 запросы при загрузке позиций и материалов
- ❌ Нет кэширования списка смет
- ❌ Полная перезагрузка при обновлении одной позиции
- ✅ Используется repository pattern
- ✅ Tenant isolation работает

---

## 🎯 ПЛАН ОПТИМИЗАЦИИ

### **Фаза 1: Оптимизация справочников (Priority: HIGH)**

#### 1.1. Бесконечная прокрутка (Infinite Scroll)

**Вместо:**
```javascript
// Загружаем ВСЕ 50,000 материалов
pageSize: 50000
```

**Делаем:**
```javascript
// Загружаем по 100 материалов, подгружаем при скролле
pageSize: 100
page: 1, 2, 3...
```

**Преимущества:**
- ✅ Первая загрузка: **100 записей вместо 50,000** → 50x быстрее
- ✅ Payload: **~50 KB вместо 5 MB** → 100x меньше
- ✅ Мгновенный первый рендер
- ✅ Подгрузка следующих 100 при достижении конца списка

**Реализация:**
- Использовать `react-infinite-scroll-component`
- Кэшировать загруженные страницы в SWR
- Backend уже поддерживает пагинацию ✅

**Файлы для изменения:**
- `app/references/materials/index.jsx`
- `app/references/works/index.jsx`
- `shared/lib/api/materials.js`
- `shared/lib/api/works.js`

---

#### 1.2. Prefetching при поиске

**Проблема:** При вводе в поиск "штукатурка" - делаем новый запрос

**Решение:**
```javascript
// Предзагружаем популярные категории
useEffect(() => {
  // Prefetch популярных категорий в фоне
  materialsAPI.getAll({ category: 'Отделочные', pageSize: 100 });
  materialsAPI.getAll({ category: 'Строительные', pageSize: 100 });
}, []);
```

**Преимущества:**
- ✅ Мгновенная фильтрация по категориям
- ✅ Используем idle time браузера

---

#### 1.3. Service Worker кэширование

**Добавить:**
```javascript
// public/sw.js - кэшируем справочники на 1 час
self.addEventListener('fetch', (event) => {
  if (event.request.url.includes('/api/materials?isGlobal=true')) {
    event.respondWith(
      caches.open('references-v1').then(cache => {
        return cache.match(event.request).then(response => {
          return response || fetch(event.request).then(fetchResponse => {
            cache.put(event.request, fetchResponse.clone());
            return fetchResponse;
          });
        });
      })
    );
  }
});
```

**Преимущества:**
- ✅ Работает офлайн
- ✅ Мгновенная загрузка при повторном визите
- ✅ Не требует изменений в коде

---

#### 1.4. IndexedDB для больших справочников

**Для пользователей с большими базами (10K+ материалов):**
```javascript
// Сохраняем в IndexedDB вместо памяти
import { openDB } from 'idb';

const db = await openDB('smeta-references', 1, {
  upgrade(db) {
    db.createObjectStore('materials', { keyPath: 'id' });
  },
});

// Сохраняем при загрузке
await db.put('materials', material);

// Читаем без запроса к серверу
const materials = await db.getAll('materials');
```

**Преимущества:**
- ✅ Не занимает RAM браузера
- ✅ Персистентно между сессиями
- ✅ Поддержка до 50 MB данных

---

### **Фаза 2: Оптимизация смет (Priority: MEDIUM)**

#### 2.1. Lazy loading позиций сметы

**Проблема:**
```javascript
// Загружаем ВСЕ позиции сметы сразу (может быть 1000+)
GET /estimates/123/items
```

**Решение:**
```javascript
// Загружаем только видимые секции
GET /estimates/123/sections  // Только структура секций
GET /estimates/123/sections/abc123/items?page=1  // Позиции по секциям
```

**Преимущества:**
- ✅ Быстрая загрузка сметы с 1000+ позициями
- ✅ Пользователь видит структуру мгновенно
- ✅ Позиции подгружаются при раскрытии секции

---

#### 2.2. Optimistic Updates

**Сейчас:**
```javascript
// Ждём ответ сервера перед обновлением UI
await materialsAPI.update(id, data);
fetchMaterials(); // Перезагружаем ВСЕ
```

**Делаем:**
```javascript
// Обновляем UI сразу, откатываем если ошибка
mutate(
  '/materials',
  async (currentData) => {
    // Оптимистично обновляем
    const updated = currentData.map(m => 
      m.id === id ? { ...m, ...data } : m
    );
    
    // Отправляем на сервер в фоне
    materialsAPI.update(id, data).catch(() => {
      // Откатываем при ошибке
      mutate('/materials', currentData, false);
    });
    
    return updated;
  },
  { revalidate: false }
);
```

**Преимущества:**
- ✅ Мгновенный UI
- ✅ Не ждём сервера
- ✅ Автоматический rollback при ошибке

---

#### 2.3. Batch обновления

**Проблема:** При добавлении 10 материалов в смету - 10 запросов

**Решение:**
```javascript
// Батчим операции
POST /estimate-items/batch
{
  "operations": [
    { "action": "create", "data": {...} },
    { "action": "update", "id": 123, "data": {...} },
    { "action": "delete", "id": 456 }
  ]
}
```

**Преимущества:**
- ✅ 1 запрос вместо N
- ✅ Транзакционность (все или ничего)
- ✅ Меньше нагрузки на сервер

---

#### 2.4. GraphQL-подобные запросы

**Вместо:**
```javascript
// 3 запроса для полной сметы
GET /estimates/123         // Смета
GET /estimates/123/items   // Позиции
GET /projects/456          // Проект
```

**Делаем:**
```javascript
// 1 запрос с указанием нужных полей
GET /estimates/123?include=items,project,sections
```

**Преимущества:**
- ✅ 1 запрос вместо 3+
- ✅ Меньше round-trips
- ✅ Уже частично реализовано в `/estimates/:id/full` ✅

---

### **Фаза 3: Backend оптимизации (Priority: LOW, но высокий эффект)**

#### 3.1. Database индексы

**Проверить наличие индексов:**
```sql
-- Должны быть индексы на:
CREATE INDEX idx_materials_tenant_global ON materials(tenant_id, is_global);
CREATE INDEX idx_materials_category ON materials(category);
CREATE INDEX idx_materials_search ON materials USING gin(to_tsvector('russian', name));
CREATE INDEX idx_estimate_items_estimate ON estimate_items(estimate_id);
CREATE INDEX idx_estimate_items_tenant ON estimate_items(tenant_id);
```

**Эффект:** Запросы в **10-50 раз быстрее** на больших таблицах

---

#### 3.2. Connection pooling

**Добавить в database.js:**
```javascript
const pool = new Pool({
  connectionString: process.env.DATABASE_URL,
  max: 20,                    // Было 10 по умолчанию
  idleTimeoutMillis: 30000,
  connectionTimeoutMillis: 5000,
});
```

**Эффект:** Меньше задержек при параллельных запросах

---

#### 3.3. Redis кэш (будущее)

**Для масштабирования:**
```javascript
import Redis from 'ioredis';
const redis = new Redis(process.env.REDIS_URL);

// Кэшируем на 1 час
await redis.setex('materials:global', 3600, JSON.stringify(materials));
```

**Когда нужно:**
- Более 100 пользователей
- Нужна синхронизация между несколькими инстансами
- Переход на Render Standard+ ($25/мес)

---

## 📊 Приоритизация

### **Быстрые победы (1-2 часа работы):**

1. ✅ **Infinite Scroll для справочников** → -90% времени загрузки
2. ✅ **Optimistic Updates** → Мгновенный UI
3. ✅ **Проверка DB индексов** → +50% скорость запросов

### **Средние задачи (4-6 часов):**

4. ⏳ **Lazy loading позиций смет** → -70% времени открытия сметы
5. ⏳ **Batch операции** → -80% запросов при массовых действиях
6. ⏳ **Service Worker кэширование** → Офлайн режим

### **Долгосрочные (1-2 дня):**

7. 🔮 **IndexedDB** → Работа с огромными справочниками
8. 🔮 **GraphQL-style API** → Гибкость запросов
9. 🔮 **Redis кэш** → Для scaling (когда вырастете)

---

## 🎯 Рекомендуемый порядок внедрения

### **Сейчас (сегодня-завтра):**
1. Infinite Scroll для Materials
2. Optimistic Updates для редактирования

### **На этой неделе:**
3. Проверка DB индексов
4. Lazy loading позиций смет

### **В январе:**
5. Service Worker
6. Batch операции

### **Когда появятся первые 10 клиентов:**
7. Connection pooling optimization
8. Monitoring (New Relic / Datadog)

### **При 100+ пользователей:**
9. Redis
10. CDN для статики (Cloudflare)
11. Render Standard tier

---

## 📈 Ожидаемые результаты

### **После Фазы 1 (Справочники):**
- ⚡ Время первой загрузки: **5 сек → 0.3 сек** (-95%)
- 📦 Размер payload: **5 MB → 50 KB** (-99%)
- 💾 Использование RAM: **50 MB → 2 MB** (-96%)
- ✨ User Experience: **Мгновенная реакция**

### **После Фазы 2 (Сметы):**
- ⚡ Открытие сметы: **2 сек → 0.5 сек** (-75%)
- 🔄 Обновление позиции: **500ms → 50ms** (-90%, optimistic)
- 📊 Добавление 10 материалов: **5 сек → 0.5 сек** (-90%, batch)

### **После Фазы 3 (Backend):**
- 🗄️ Запросы к БД: **200ms → 20ms** (-90%, индексы)
- 🚀 Параллельные пользователи: **10 → 50+** (pooling)
- 💰 Расходы на Render: **$0 → $0** (пока на Free tier)

---

## 💰 Финансовый эффект

### **Сейчас на Free tier:**
- Cold starts каждые 15 минут
- Медленные запросы
- Ограничение 512 MB RAM

### **После оптимизаций:**
- Можно оставаться на **Free tier дольше** (до 50 пользователей вместо 10)
- Экономия **$14/мес** на отложенном апгрейде
- При апгрейде на Starter - **в 2 раза больше capacity**

---

## 🛠️ Инструменты для мониторинга

### **Добавить в проект:**

1. **React DevTools Profiler** - найти медленные компоненты
2. **Chrome Performance tab** - анализ загрузки
3. **Lighthouse** - метрики производительности
4. **Sentry** - мониторинг ошибок (бесплатный tier)
5. **LogRocket** - session replay для дебага

### **Метрики для отслеживания:**

```javascript
// Добавить в каждый API запрос
console.time('materialsAPI.getAll');
const materials = await materialsAPI.getAll();
console.timeEnd('materialsAPI.getAll');

// Performance API
performance.mark('start-materials-load');
// ... загрузка
performance.mark('end-materials-load');
performance.measure('materials-load', 'start-materials-load', 'end-materials-load');
```

---

## 🎓 Обучающие ресурсы

- [React Performance Optimization](https://react.dev/learn/render-and-commit)
- [SWR Documentation](https://swr.vercel.app/docs/advanced/performance)
- [PostgreSQL Indexing](https://www.postgresql.org/docs/current/indexes.html)
- [Service Workers Guide](https://developers.google.com/web/fundamentals/primers/service-workers)

---

**Автор**: AI Assistant  
**Версия**: 1.0  
**Последнее обновление**: 20 декабря 2025
