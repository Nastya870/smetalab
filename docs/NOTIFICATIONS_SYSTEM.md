# 🔔 Система уведомлений Smeta Pro

**Дата:** 28 декабря 2025  
**Версия:** 1.0

## 📋 Обзор

Реализована полноценная система уведомлений с:
- ✅ **Toast-уведомления** (всплывающие справа вверху)
- ✅ **Центр уведомлений** (в Header, выпадающий список)
- ✅ **Хранение истории** (localStorage, до 50 уведомлений)
- ✅ **Красивый дизайн** (градиенты, иконки, анимации)

---

## 🎨 Компоненты

### 1. **NotificationsContext** (`shared/lib/contexts/NotificationsContext.jsx`)

Центральный контекст для управления уведомлениями.

**API:**
```javascript
const {
  notifications,      // Массив всех уведомлений
  unreadCount,        // Количество непрочитанных
  addNotification,    // Добавить уведомление (низкоуровневый)
  success,            // Уведомление об успехе
  error,              // Уведомление об ошибке
  warning,            // Предупреждение
  info,               // Информация
  markAsRead,         // Пометить как прочитанное
  markAllAsRead,      // Пометить все
  removeNotification, // Удалить одно
  clearAll            // Очистить все
} = useNotifications();
```

**Типы уведомлений:**
- `success` - Успешные операции (зеленый)
- `error` - Ошибки (красный)
- `warning` - Предупреждения (оранжевый)
- `info` - Информация (синий)

**Категории:**
```javascript
NOTIFICATION_CATEGORIES = {
  PROJECT: 'project',
  ESTIMATE: 'estimate',
  MATERIAL: 'material',
  WORK: 'work',
  PURCHASE: 'purchase',
  USER: 'user',
  SYSTEM: 'system'
}
```

---

### 2. **CustomToast** (`shared/ui/components/CustomToast.jsx`)

Кастомный компонент для Toast-уведомлений.

**Особенности:**
- 🎨 Градиентная верхняя полоса (индикатор типа)
- 🔵 Цветная иконка в круглой плашке
- 📝 Читаемый текст (400-символов)
- ❌ Кнопка закрытия
- 🌊 Тени и border для объёма

**Цветовая схема:**
| Тип     | Градиент                                  | Основной цвет |
|---------|-------------------------------------------|---------------|
| Success | `#667eea → #764ba2` (фиолетово-синий)    | `#10B981`     |
| Error   | `#f093fb → #f5576c` (розово-красный)     | `#EF4444`     |
| Warning | `#FFD89B → #F59E0B` (желто-оранжевый)    | `#F59E0B`     |
| Info    | `#89f7fe → #66a6ff` (голубой)            | `#3B82F6`     |

---

### 3. **NotificationList** (`app/layout/MainLayout/Header/NotificationSection/NotificationList.jsx`)

Список уведомлений в Header.

**Функционал:**
- 📜 Скроллируемый список (max-height: calc(100vh - 140px))
- 🔔 Badge "Новое" на непрочитанных
- 🕒 Относительное время (date-fns: "5 минут назад")
- 📁 Иконка категории (проект/смета/материал)
- ❌ Кнопка удаления каждого уведомления
- 🖱️ Клик = пометить прочитанным

**Empty State:**
- Пустой список = иконка + текст "Нет уведомлений"

---

### 4. **NotificationSection** (`app/layout/MainLayout/Header/NotificationSection/index.jsx`)

Иконка-колокольчик в Header с dropdown.

**Обновления:**
- 🔴 Badge с количеством непрочитанных
- 🔔 Заголовок "Уведомления" (вместо "All Notification")
- ✅ Кнопка "Отметить все" (вместо "Mark as all read")
- 🗑️ Убран фильтр (не нужен на данном этапе)

---

## 🚀 Использование

### Быстрый старт

```javascript
import { useNotifications, NOTIFICATION_CATEGORIES } from 'shared/lib/contexts/NotificationsContext';

function MyComponent() {
  const { success, error } = useNotifications();

  const handleSave = async () => {
    try {
      await api.save(data);
      success('Сохранено', 'Данные успешно сохранены', {
        category: NOTIFICATION_CATEGORIES.PROJECT
      });
    } catch (err) {
      error('Ошибка', err.message, {
        category: NOTIFICATION_CATEGORIES.PROJECT
      });
    }
  };

  return <Button onClick={handleSave}>Сохранить</Button>;
}
```

---

### Замена старого Snackbar

**До:**
```javascript
const [snackbar, setSnackbar] = useState({ open: false, message: '', severity: 'info' });

const showSnackbar = (message, severity) => {
  setSnackbar({ open: true, message, severity });
};

// JSX
<Snackbar open={snackbar.open} ...>
  <Alert severity={snackbar.severity}>{snackbar.message}</Alert>
</Snackbar>
```

**После:**
```javascript
const { success, error, warning, info } = useNotifications();

// Просто вызов
success('Проект создан', 'Новый проект добавлен в систему', {
  category: NOTIFICATION_CATEGORIES.PROJECT
});

// Никакого JSX не нужно!
```

---

### Параметры уведомлений

```javascript
// Полный формат
success(
  'Заголовок',           // Краткий заголовок (обязательно)
  'Подробное описание',  // Детали (обязательно)
  {
    category: NOTIFICATION_CATEGORIES.PROJECT,  // Категория (опционально)
    showToast: true,                            // Показать toast (по умолчанию true)
    action: { label: 'Открыть', url: '/projects/123' }  // Действие (пока не реализовано)
  }
);

// Минимальный формат (только toast, без истории)
success('Сохранено');
```

---

## 📦 Интеграция в страницы

### ✅ Готово

| Страница   | Статус | События                                      |
|------------|--------|----------------------------------------------|
| Projects   | ✅     | create, update, delete                       |

### 🔨 Требуется интеграция

| Страница   | Файл                                | События                                      |
|------------|-------------------------------------|----------------------------------------------|
| Materials  | `app/references/materials/index.jsx`| create, update, delete, import               |
| Works      | `app/references/works/index.jsx`    | create, update, delete                       |
| Estimates  | `app/estimates/...`                 | create, update, delete, publish              |
| Purchases  | `app/estimates/Purchases.jsx`       | create, update, link to material             |

**Шаблон замены:**

1. Добавить import:
```javascript
import { useNotifications, NOTIFICATION_CATEGORIES } from 'shared/lib/contexts/NotificationsContext';
```

2. Получить методы:
```javascript
const { success, error } = useNotifications();
```

3. Заменить `showSnackbar`:
```javascript
// Было
showSnackbar('Материал создан', 'success');

// Стало
success('Материал создан', 'Новый материал добавлен в справочник', {
  category: NOTIFICATION_CATEGORIES.MATERIAL
});
```

4. Удалить старый Snackbar:
```javascript
// Удалить
const [snackbar, setSnackbar] = useState({ ... });
<Snackbar ...><Alert ...>...</Alert></Snackbar>
```

---

## 🎨 Дизайн-система

### Цвета

| Тип     | Background | Border    | Icon/Text |
|---------|------------|-----------|-----------|
| Success | `#ECFDF5`  | `#34D399` | `#10B981` |
| Error   | `#FEF2F2`  | `#F87171` | `#EF4444` |
| Warning | `#FFFBEB`  | `#FBBF24` | `#F59E0B` |
| Info    | `#EFF6FF`  | `#60A5FA` | `#3B82F6` |

### Типография

| Элемент         | Стиль                           |
|-----------------|---------------------------------|
| Toast message   | `body2`, 14px, fontWeight 500   |
| List title      | `subtitle2`, 13px, fontWeight 600 |
| List message    | `body2`, 13px, color secondary  |
| List timestamp  | `caption`, 12px, color disabled |

### Иконки (Tabler Icons)

| Тип     | Иконка              |
|---------|---------------------|
| Success | `IconCheck`         |
| Error   | `IconAlertCircle`   |
| Warning | `IconAlertTriangle` |
| Info    | `IconInfoCircle`    |

**Категории:**
| Категория | Иконка               |
|-----------|----------------------|
| Project   | `IconBriefcase`      |
| Estimate  | `IconFileText`       |
| Material  | `IconPackage`        |
| Work      | `IconTool`           |
| Purchase  | `IconShoppingCart`   |
| User      | `IconUser`           |
| System    | `IconInfoCircle`     |

---

## 📁 Структура файлов

```
shared/lib/contexts/
  └── NotificationsContext.jsx         # Context + hooks

shared/ui/components/
  └── CustomToast.jsx                  # Toast component

app/layout/MainLayout/Header/NotificationSection/
  ├── index.jsx                        # Dropdown wrapper
  └── NotificationList.jsx             # List component

app/
  ├── App.jsx                          # NotificationsProvider
  └── projects/index.jsx               # Пример использования
```

---

## 🔧 Технические детали

### localStorage

**Ключ:** `smeta_notifications`

**Формат:**
```json
[
  {
    "id": 1735379234567.123,
    "title": "Проект создан",
    "message": "Новый проект добавлен",
    "type": "success",
    "category": "project",
    "read": false,
    "createdAt": "2025-12-28T10:20:34.567Z"
  }
]
```

**Лимиты:**
- Максимум 50 уведомлений
- Автоматическое удаление старых при превышении
- Очистка при `clearAll()`

---

### Производительность

- ✅ `useCallback` для всех обработчиков
- ✅ `useMemo` для debounce
- ✅ Минимальные re-renders через Context
- ✅ localStorage синхронизация только при изменении массива

---

## 🐛 Troubleshooting

### Toast не показывается

**Проблема:** Уведомление в списке есть, toast не появился.

**Решение:**
```javascript
// Проверьте showToast
success('Title', 'Message', { showToast: true }); // явно true
```

### Уведомления не сохраняются

**Проблема:** После перезагрузки страницы список пуст.

**Решение:**
1. Проверьте консоль на ошибки localStorage
2. Очистите localStorage: `localStorage.removeItem('smeta_notifications')`
3. Проверьте лимит хранилища браузера

### Badge не обновляется

**Проблема:** Прочитал уведомление, но badge всё равно показывает старое число.

**Решение:**
- Context обновляется автоматически через `markAsRead`
- Проверьте, что компонент обёрнут в `NotificationsProvider`

---

## 📝 TODO (будущие улучшения)

- [ ] **Actions** - кнопки "Открыть", "Отменить" в уведомлении
- [ ] **Группировка** - сгруппировать по категориям или дате
- [ ] **Фильтры** - "Только непрочитанные", "Только проекты"
- [ ] **Push Notifications** - реальные push через Service Worker
- [ ] **Sound** - звук при новом уведомлении (опционально)
- [ ] **Persistence** - синхронизация с backend API

---

## ✅ Чеклист миграции страницы

1. [ ] Импортировать `useNotifications` и `NOTIFICATION_CATEGORIES`
2. [ ] Получить методы: `const { success, error } = useNotifications()`
3. [ ] Заменить все `showSnackbar` на `success`/`error`
4. [ ] Удалить `useState` для snackbar
5. [ ] Удалить `<Snackbar><Alert>...</Alert></Snackbar>` из JSX
6. [ ] Добавить категорию в опции (optional)
7. [ ] Протестировать: create, update, delete операции
8. [ ] Проверить Header badge и список уведомлений

---

**Автор:** AI Assistant  
**Контакт:** GitHub Issues  
**Лицензия:** MIT
