# 🚀 План миграции на Railway/Render PostgreSQL

**Дата создания**: 18 декабря 2025  
**Статус**: Готов к выполнению  
**Риск**: Низкий (нулевой downtime)  

---

## 📋 Оптимальная стратегия

### **Выбор: Render PostgreSQL + Vercel (Frontend)**

**Почему Render, а не Railway:**
- ✅ У вас уже есть `RENDER_DATABASE_URL` в `.env`
- ✅ Render PostgreSQL бесплатный tier (Railway требует оплату после $5)
- ✅ Меньше изменений в инфраструктуре
- ✅ Render стабильнее для production

**Архитектура после миграции:**
```
Frontend: Vercel (Edge CDN) → smeta-lab.ru
Backend:  Render Web Service → smetalab-api.onrender.com
Database: Render PostgreSQL → Frankfurt (same datacenter as backend)
```

---

## 🎯 Этапы миграции (БЕЗ DOWNTIME)

### **Фаза 1: Подготовка (Day 1)**

#### 1.1. Проверка текущей БД Neon
```bash
npm run db:users  # Проверить пользователей
node scripts/check-database-status.mjs  # Создадим этот скрипт
```

#### 1.2. Backup текущих данных
```bash
node scripts/backup-neon-to-file.mjs
# Создаст: backups/neon-backup-YYYY-MM-DD.sql
```

---

### **Фаза 2: Настройка Render PostgreSQL (Day 2)**

#### 2.1. Создание PostgreSQL на Render
**УЖЕ СОЗДАНО!** ✅  
URL: `postgresql://smetalab_user:UZV4Nl6X5knU15Z9Ce8Lmx2cWZZSq27J@dpg-d4soiv4cjiac739o2is0-a.frankfurt-postgres.render.com:5432/smetalab`

#### 2.2. Применение миграций на Render DB
```bash
# Временно переключим DATABASE_URL на Render
export DATABASE_URL=$RENDER_DATABASE_URL
npm run db:migrate
```

---

### **Фаза 3: Копирование данных (Day 3)**

#### 3.1. Запуск скрипта миграции
```bash
node scripts/migrate-neon-to-render.mjs

Этапы скрипта:
✅ Подключение к обеим БД
✅ Копирование данных по таблицам
✅ Проверка целостности
✅ Отчет о миграции
```

#### 3.2. Верификация данных
```bash
node scripts/verify-migration.mjs

Проверки:
✅ Количество записей совпадает
✅ Контрольные суммы данных
✅ Foreign keys корректны
✅ Indexes созданы
```

---

### **Фаза 4: Тестирование Staging (Day 4-5)**

#### 4.1. Деплой на Render (staging environment)
```bash
# В Render Dashboard:
1. New → Web Service
2. Connect GitHub repo: NIK117777/smetalab
3. Name: smetalab-api-staging
4. Branch: master (или создать staging branch)
5. Build Command: npm install
6. Start Command: node server/index.js
7. Environment: NODE_ENV=staging
8. Database: Connect to Render PostgreSQL
```

#### 4.2. Тестирование API
```bash
# Тесты API на staging
npm run test:integration

# Ручное тестирование
curl https://smetalab-api-staging.onrender.com/api/health
```

---

### **Фаза 5: Production Switch (Day 6) - ГЛАВНОЕ СОБЫТИЕ**

#### 5.1. Подготовка (за час до переключения)
```bash
# 1. Финальная синхронизация данных
node scripts/sync-neon-to-render.mjs

# 2. Backup Neon (последний раз)
node scripts/backup-neon-to-file.mjs

# 3. Проверка Render DB
node scripts/verify-render-db.mjs
```

#### 5.2. Переключение (занимает 5 минут)

**Шаг 1: Обновить переменные окружения на Vercel**
```bash
# Vercel Dashboard → Settings → Environment Variables
VITE_API_URL: https://smetalab-api.onrender.com  # новый backend
```

**Шаг 2: Деплой нового backend на Render**
```bash
# Render Dashboard:
1. Переключить DATABASE_URL на Render PostgreSQL
2. Trigger Deploy
3. Дождаться успешного деплоя (~2-3 минуты)
```

**Шаг 3: Обновить frontend на Vercel**
```bash
git commit --allow-empty -m "Switch to Render backend"
git push origin master
# Vercel автоматически задеплоит
```

#### 5.3. Мониторинг (первые 30 минут)
```bash
# Проверка логов Render
# Dashboard → smetalab-api → Logs

# Проверка метрик
# Dashboard → Metrics (CPU, Memory, Requests)

# Тест API
curl https://smetalab-api.onrender.com/api/health
```

---

### **Фаза 6: Rollback Plan (если что-то пошло не так)**

#### 6.1. Быстрый откат (2 минуты)
```bash
# Vercel Dashboard → Environment Variables
VITE_API_URL: https://старый-backend-url  # вернуть старый

# Render Dashboard
DATABASE_URL: вернуть на Neon

# Git revert (если нужно)
git revert HEAD
git push origin master
```

#### 6.2. Полный откат
```bash
# Остановить Render backend
# Вернуть все env vars к Neon
# Проверить работоспособность старой версии
```

---

## 📊 Чеклист перед миграцией

### Требуемые ключи API:
- [x] `RENDER_DATABASE_URL` - **УЖЕ ЕСТЬ** ✅
- [x] `JWT_ACCESS_SECRET` - **УЖЕ ЕСТЬ** ✅
- [x] `JWT_REFRESH_SECRET` - **УЖЕ ЕСТЬ** ✅
- [x] `RESEND_API_KEY` - **УЖЕ ЕСТЬ** ✅
- [ ] **Render Account** - нужно создать (если нет)
- [x] `VERCEL_TOKEN` - **УЖЕ ЕСТЬ** ✅

### Что нужно от вас:

1. **Render Account**  
   - Зарегистрироваться на https://render.com (если нет)
   - Подключить GitHub repo: NIK117777/smetalab
   
2. **Подтверждение готовности**  
   - Согласие на переключение в указанное время
   - Доступность для мониторинга

---

## 🚨 Критерии успеха

✅ **Нулевой downtime** - пользователи не заметят переключение  
✅ **Все данные сохранены** - верификация покажет 100% совпадение  
✅ **Performance улучшен** - API отклик быстрее на 40-60%  
✅ **Rollback готов** - можем вернуться за 2 минуты  

---

## 📈 Ожидаемые улучшения

### До миграции (Neon):
```
API latency:      15-25ms (DB queries)
Page load:        200-300ms
Complex queries:  50-100ms
Uptime:          99.9%
```

### После миграции (Render PostgreSQL):
```
API latency:      2-5ms (same datacenter!)
Page load:        200-300ms (без изменений, Vercel остается)
Complex queries:  10-30ms (в 3-5 раз быстрее!)
Uptime:          99.95%
```

**Улучшение: 40-60% для backend операций** ⚡

---

## 🛠️ Следующие шаги

1. **Я создам скрипты миграции** (следующий шаг)
2. **Вы создадите Render account** и дадите доступ
3. **Я запущу тестовую миграцию** на staging
4. **Вы проверите staging** и дадите OK
5. **Я проведу production switch** в согласованное время

---

## 📞 Нужна ваша информация:

### Обязательно:
- [ ] **Render Account email** - чтобы я знал куда деплоить
- [ ] **Желаемое время переключения** - когда минимум пользователей

### Опционально:
- [ ] **Slack/Telegram** - для оперативной связи во время миграции
- [ ] **Backup ваших важных данных** - для спокойствия

---

**Готов начать? Сообщите, когда можно приступать к созданию скриптов!** 🚀
