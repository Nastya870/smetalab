# 🔍 Анализ монолитных компонентов проекта Smeta Pro

**Дата**: 26 декабря 2025  
**Статус**: 📊 Аналитический отчёт (БЕЗ внесения изменений)

## 📊 ТОП-20 самых больших файлов

| # | Файл | Строк | Тип | Критичность |
|---|------|-------|-----|-------------|
| 1 | `shared/lib/ks3Borders.js` | **6289** | Utility | 🔴 КРИТИЧНО |
| 2 | `app/estimates/EstimateWithSidebar.jsx` | **3442** | Component | 🔴 КРИТИЧНО |
| 3 | `app/estimates/Purchases.jsx` | 1916 | Component | 🟡 Высокая |
| 4 | `shared/lib/exportKS2Excel.js` | 1485 | Utility | 🟡 Высокая |
| 5 | `app/estimates/SpecialistEstimate.jsx` | 1387 | Component | 🟡 Высокая |
| 6 | `app/estimates/WorkCompletionActs.jsx` | 1326 | Component | 🟡 Высокая |
| 7 | `app/references/materials/index.jsx` | 1138 | Component | 🟢 Средняя |
| 8 | `app/estimates/ContractView.jsx` | 1092 | Component | 🟢 Средняя |
| 9 | `app/estimates/ObjectParameters.jsx` | 1014 | Component | 🟢 Средняя |
| 10 | `app/purchases/GlobalPurchases.jsx` | 966 | Component | 🟢 Средняя |

---

## 🔴 КРИТИЧНО: EstimateWithSidebar.jsx (3442 строки)

### Структура компонента

**Основные проблемы**:
- 🔴 **30+ состояний** (useState) в одном компоненте
- 🔴 **50+ функций-обработчиков** 
- 🔴 **Множество ответственностей** (God Object anti-pattern)
- 🔴 Сложность тестирования и поддержки
- 🔴 Длинная цепочка prop drilling

### Выявленные ответственности (7 доменов):

#### 1️⃣ **Управление работами** (Works Management)
```javascript
// State (8 переменных)
- availableWorks, loadingWorks, errorWorks
- transferringWorks, addingWorkId
- sidebarVisible, searchTerm, selectedSection
- workSourceTab (global/tenant)

// Functions (5 функций)
- loadWorksCached()
- handleTransferToEstimate()
- handleDeleteWork()
- handleWorkQuantityChange()
- handleWorkPriceChange()

// Cache
- worksCache, worksCacheTimestamp
```

**Потенциал выделения**: ✅ **ВЫСОКИЙ**  
**Рекомендация**: Создать `useWorksManagement` hook или отдельный компонент `WorksPanel`

---

#### 2️⃣ **Управление материалами** (Materials Management)
```javascript
// State (11 переменных)
- materialDialogOpen, materialDialogMode
- currentWorkItem, materialToReplace
- allMaterialsForDialog, loadingMaterials
- materialSearchQuery
- materialsPage, materialsHasMore, materialsTotalRecords
- MATERIALS_PAGE_SIZE

// Functions (10 функций)
- loadMaterialsForDialog()
- handleOpenAddMaterial()
- handleOpenReplaceMaterial()
- handleAddMaterialToWork()
- handleReplaceMaterialConfirm()
- handleDeleteMaterial()
- handleMaterialQuantityChange()
- handleMaterialConsumptionChange()
- handleMaterialSearchChange()
- loadMoreMaterials()

// Cache
- materialsCache, materialsCacheTimestamp
```

**Потенциал выделения**: ✅ **ОЧЕНЬ ВЫСОКИЙ**  
**Рекомендация**: Создать `MaterialsDialog` компонент + `useMaterialsManagement` hook

---

#### 3️⃣ **Ценообразование** (Pricing & Coefficients)
```javascript
// State (3 переменные)
- coefficientModalOpen
- currentCoefficient
- originalPrices (Map)

// Functions (3 функции)
- saveOriginalPrices()
- handleApplyCoefficient()
- handleResetPrices()
```

**Потенциал выделения**: ✅ **ВЫСОКИЙ**  
**Рекомендация**: `PriceCoefficientModal` уже вынесен, но логика внутри родителя. Перенести в сам модал.

---

#### 4️⃣ **Экспорт и шаблоны** (Export & Templates)
```javascript
// State (5 переменных)
- exportingExcel
- saveTemplateDialogOpen
- templateFormData
- savingTemplate

// Functions (4 функции)
- handleExportExcel()
- handleSaveAsTemplate()
- handleSaveTemplateConfirm()
- handleTemplateFormChange()
```

**Потенциал выделения**: ✅ **СРЕДНИЙ**  
**Рекомендация**: Создать `useEstimateExport` hook

---

#### 5️⃣ **Параметры объекта** (Object Parameters)
```javascript
// State (1 переменная)
- parametersWidgetOpen

// Component
- ObjectParametersSidebar (уже вынесен)
```

**Потенциал выделения**: ✅ **УЖЕ ВЫДЕЛЕН**  
**Рекомендация**: Всё хорошо, только состояние открытия в родителе.

---

#### 6️⃣ **Данные сметы** (Estimate Data)
```javascript
// State (5 переменных)
- estimateData
- deferredEstimateData
- estimateMetadata
- loading, saving

// Refs (3 переменных)
- savedEstimateDataRef
- isInitialLoadRef
- isInitialLoadComplete

// Functions (3 функции)
- loadEstimateData()
- saveEstimate()
- handleClearEstimate()
```

**Потенциал выделения**: ✅ **СРЕДНИЙ**  
**Рекомендация**: Создать `useEstimateData` hook

---

#### 7️⃣ **UI состояния** (UI State)
```javascript
// State (4 переменных)
- hasUnsavedChanges
- filtersPanelOpen
- editingValuesRef (для uncontrolled inputs)

// Functions
- toggleSidebar()
- onUnsavedChangesRef
```

**Потенциал выделения**: ✅ **НИЗКИЙ**  
**Рекомендация**: Оставить в компоненте как координатор.

---

## 🎯 План рефакторинга EstimateWithSidebar.jsx

### Фаза 1: Выделение кастомных хуков (Custom Hooks)

```
app/estimates/EstimateWithSidebar/
├── index.jsx                        # Главный компонент (координатор)
├── hooks/
│   ├── useWorksManagement.js        # ✅ Работы
│   ├── useMaterialsManagement.js    # ✅ Материалы
│   ├── useEstimateData.js           # ✅ Данные сметы
│   ├── useEstimateExport.js         # ✅ Экспорт/шаблоны
│   └── usePriceCoefficient.js       # ✅ Коэффициент цен
├── components/
│   ├── WorksPanel.jsx               # Панель работ (sidebar)
│   ├── MaterialsDialog.jsx          # Диалог выбора материалов
│   ├── EstimateTable.jsx            # Таблица сметы
│   ├── WorkRow.jsx                  # ✅ Уже вынесен
│   ├── MaterialRow.jsx              # ✅ Уже вынесен
│   └── SummaryFooter.jsx            # Итоги сметы
└── utils/
    ├── sorting.js                   # compareWorkItems, sortWorkItems
    └── calculations.js              # Расчёты (totals, weight)
```

### Результат после рефакторинга:

**ДО**:
- 1 файл: 3442 строки
- 30+ состояний
- 50+ функций
- Сложность поддержки: 🔴 Очень высокая

**ПОСЛЕ**:
- Главный файл: ~300-400 строк (координатор)
- 5 хуков: ~200-300 строк каждый
- 6 компонентов: ~100-200 строк каждый
- Сложность поддержки: 🟢 Низкая

---

## 🔴 КРИТИЧНО: ks3Borders.js (6289 строк)

### Анализ

```javascript
export function applyBorders(worksheet) {
  // 6289 строк одной функции!!!
  // Применяет границы к ячейкам Excel для КС-3
}
```

**Проблемы**:
- 🔴 **6289 строк в ОДНОЙ функции** (мировой рекорд!)
- 🔴 Генерируемый код (вероятно автоматическая генерация)
- 🔴 Невозможность понять логику без генератора
- 🔴 Огромный размер бандла

### Решение:

#### Вариант 1: Разбить на секции
```javascript
// ks3Borders/
// ├── index.js
// ├── headerBorders.js    (строки 1-1000)
// ├── bodyBorders.js      (строки 1001-3000)
// ├── footerBorders.js    (строки 3001-5000)
// └── utilityBorders.js   (строки 5001-6289)

export function applyBorders(worksheet) {
  applyHeaderBorders(worksheet);
  applyBodyBorders(worksheet);
  applyFooterBorders(worksheet);
  applyUtilityBorders(worksheet);
}
```

#### Вариант 2: Шаблонизация (ЛУЧШЕ!)
```javascript
// Вместо генерации кода использовать JSON-схему
const ks3BordersSchema = {
  header: {
    rows: [
      { cells: ['A1', 'B1'], border: 'thin', color: 'black' },
      // ...
    ]
  },
  body: { /* ... */ },
  footer: { /* ... */ }
};

export function applyBorders(worksheet) {
  applyBordersFromSchema(worksheet, ks3BordersSchema);
}
```

**Потенциал сокращения**: 6289 строк → **~500 строк** (92% сокращение!)

---

## 🟡 Purchases.jsx (1916 строк)

### Структура

**Ответственности** (4 домена):

1. **Управление закупками** (Purchase Management)
   - Создание, редактирование, удаление закупок
   - Статусы закупок
   
2. **Управление позициями закупок** (Purchase Items)
   - Добавление/удаление материалов
   - Изменение количества
   
3. **Поставщики** (Suppliers)
   - Выбор поставщика
   - Группировка по поставщикам
   
4. **Экспорт** (Export)
   - Excel экспорт закупок

### План рефакторинга:

```
app/estimates/Purchases/
├── index.jsx                    # Координатор
├── hooks/
│   ├── usePurchases.js          # Логика закупок
│   ├── usePurchaseItems.js      # Логика позиций
│   └── usePurchaseExport.js     # Экспорт
└── components/
    ├── PurchasesList.jsx        # Список закупок
    ├── PurchaseForm.jsx         # Форма закупки
    ├── PurchaseItemsTable.jsx   # Таблица позиций
    └── SupplierSelector.jsx     # Выбор поставщика
```

**Результат**: 1916 строк → ~400 строк (главный) + 4 хука + 4 компонента

---

## 🟡 SpecialistEstimate.jsx (1387 строк)

### Структура

**Ответственности**:
1. Отображение сметы для специалиста
2. Фильтрация работ по фазам/стадиям
3. Расчёт прогресса выполнения
4. Экспорт в PDF

### План рефакторинга:

```
app/estimates/SpecialistEstimate/
├── index.jsx                      # Координатор
├── hooks/
│   ├── useEstimateView.js         # Данные сметы
│   ├── useWorkFilters.js          # Фильтры работ
│   └── useProgressCalculation.js  # Прогресс
└── components/
    ├── SpecialistTable.jsx        # Таблица работ
    ├── ProgressBar.jsx            # Прогресс-бар
    └── PhaseFilter.jsx            # Фильтр по фазам
```

**Результат**: 1387 строк → ~300 строк (главный) + 3 хука + 3 компонента

---

## 📋 Итоговая матрица приоритетов

| Компонент | Строк | Приоритет | Сложность | Эффект | Рекомендация |
|-----------|-------|-----------|-----------|--------|--------------|
| `ks3Borders.js` | 6289 | 🔴 P0 | 🟡 Средняя | 🟢 Огромный | Шаблонизация → -92% кода |
| `EstimateWithSidebar.jsx` | 3442 | 🔴 P0 | 🔴 Высокая | 🟢 Огромный | Разбить на хуки + компоненты |
| `Purchases.jsx` | 1916 | 🟡 P1 | 🟢 Низкая | 🟡 Средний | Выделить хуки управления |
| `exportKS2Excel.js` | 1485 | 🟡 P1 | 🟡 Средняя | 🟡 Средний | Модульная структура |
| `SpecialistEstimate.jsx` | 1387 | 🟡 P1 | 🟢 Низкая | 🟡 Средний | Выделить хуки + компоненты |

---

## 🎯 Пошаговый план рефакторинга

### Этап 1: Quick Wins (P0, низкая сложность)
**Срок**: 1-2 дня

1. ✅ **ks3Borders.js** → Шаблонизация
   - Создать JSON-схему границ
   - Написать универсальный `applyBordersFromSchema()`
   - Результат: 6289 → ~500 строк (-92%)

### Этап 2: Critical (P0, высокая сложность)
**Срок**: 3-5 дней

2. ✅ **EstimateWithSidebar.jsx** → Декомпозиция
   - День 1: Выделить `useWorksManagement` + `WorksPanel`
   - День 2: Выделить `useMaterialsManagement` + `MaterialsDialog`
   - День 3: Выделить `useEstimateData` + `useEstimateExport`
   - День 4: Выделить `EstimateTable` + `SummaryFooter`
   - День 5: Тестирование и рефакторинг главного координатора
   - Результат: 3442 → ~400 строк (-88%)

### Этап 3: High Priority (P1)
**Срок**: 2-3 дня

3. ✅ **Purchases.jsx** → Модульная структура
   - Выделить хуки управления
   - Создать переиспользуемые компоненты
   - Результат: 1916 → ~400 строк (-79%)

4. ✅ **SpecialistEstimate.jsx** → Упрощение
   - Выделить логику фильтров
   - Создать переиспользуемые компоненты таблиц
   - Результат: 1387 → ~300 строк (-78%)

### Этап 4: Medium Priority (P2)
**Срок**: 1-2 дня

5. ✅ **exportKS2Excel.js** → Модульная структура
   - Разбить на секции (header, body, footer)
   - Вынести общие утилиты
   - Результат: 1485 → ~600 строк (-60%)

---

## 📊 Ожидаемые результаты

### Метрики ДО рефакторинга:
- **Общий размер топ-5**: 14,519 строк
- **Средняя сложность**: 🔴 Очень высокая
- **Поддерживаемость**: 🔴 Низкая
- **Тестируемость**: 🔴 Очень низкая
- **Переиспользуемость**: 🔴 Почти нулевая

### Метрики ПОСЛЕ рефакторинга:
- **Общий размер топ-5**: ~2,700 строк (**-81%**)
- **Средняя сложность**: 🟢 Низкая
- **Поддерживаемость**: 🟢 Высокая
- **Тестируемость**: 🟢 Высокая (каждый хук/компонент отдельно)
- **Переиспользуемость**: 🟢 Высокая (хуки можно использовать в других местах)

---

## 🚀 Дополнительные выгоды

### 1. Performance
- ✅ Lazy loading компонентов
- ✅ Code splitting (автоматически с React.lazy)
- ✅ Меньше перерендеров (изолированные хуки)

### 2. Developer Experience
- ✅ Проще найти нужный код
- ✅ Проще тестировать
- ✅ Проще онбординг новых разработчиков
- ✅ Меньше конфликтов в Git

### 3. Maintainability
- ✅ Легче рефакторить отдельные части
- ✅ Легче добавлять новые фичи
- ✅ Легче исправлять баги

### 4. Reusability
- ✅ Хуки можно использовать в других компонентах
- ✅ Компоненты можно использовать повторно
- ✅ Меньше дублирования кода

---

## ⚠️ Риски и митигация

### Риск 1: Поломка функциональности
**Митигация**: 
- ✅ Покрыть тестами ДО рефакторинга
- ✅ Рефакторить по одному домену
- ✅ Тестировать после каждого шага

### Риск 2: Долгий процесс
**Митигация**:
- ✅ Начать с quick wins (ks3Borders)
- ✅ Делать инкрементально (по 1 домену в день)
- ✅ Коммитить часто

### Риск 3: Несовместимость с текущим кодом
**Митигация**:
- ✅ Сохранить обратную совместимость API
- ✅ Использовать feature flags для новых компонентов
- ✅ Постепенная миграция

---

## 📝 Checklist рефакторинга одного компонента

- [ ] 1. Покрыть текущий компонент интеграционными тестами
- [ ] 2. Выявить домены ответственности (как в этом документе)
- [ ] 3. Создать структуру папок (hooks/, components/, utils/)
- [ ] 4. Выделить первый хук (самый независимый)
- [ ] 5. Покрыть хук юнит-тестами
- [ ] 6. Переместить код из компонента в хук
- [ ] 7. Проверить работу интеграционных тестов
- [ ] 8. Повторить шаги 4-7 для остальных хуков
- [ ] 9. Выделить компоненты UI
- [ ] 10. Упростить главный компонент до координатора
- [ ] 11. Финальное тестирование
- [ ] 12. Code review
- [ ] 13. Коммит и деплой

---

## 🎓 Паттерны для применения

### 1. Custom Hooks Pattern
```javascript
// До
const Component = () => {
  const [data, setData] = useState([]);
  const [loading, setLoading] = useState(false);
  
  const loadData = async () => { /* 50 строк */ };
  const updateData = () => { /* 30 строк */ };
  
  return <div>/* ... */</div>;
};

// После
const useDataManagement = () => {
  const [data, setData] = useState([]);
  const [loading, setLoading] = useState(false);
  
  const loadData = async () => { /* 50 строк */ };
  const updateData = () => { /* 30 строк */ };
  
  return { data, loading, loadData, updateData };
};

const Component = () => {
  const { data, loading, loadData, updateData } = useDataManagement();
  return <div>/* ... */</div>;
};
```

### 2. Compound Components Pattern
```javascript
// Для диалогов и модалов
<MaterialsDialog>
  <MaterialsDialog.Header />
  <MaterialsDialog.SearchBar />
  <MaterialsDialog.List />
  <MaterialsDialog.Footer />
</MaterialsDialog>
```

### 3. Container/Presenter Pattern
```javascript
// Container (логика)
const MaterialsDialogContainer = (props) => {
  const logic = useMaterialsManagement();
  return <MaterialsDialogView {...props} {...logic} />;
};

// Presenter (UI)
const MaterialsDialogView = ({ materials, onSelect }) => (
  <Dialog>/* ... */</Dialog>
);
```

---

## 🔗 Полезные ссылки

- [React Hooks Patterns](https://reactpatterns.com/)
- [Compound Components in React](https://kentcdodds.com/blog/compound-components-with-react-hooks)
- [Custom Hooks Best Practices](https://react.dev/learn/reusing-logic-with-custom-hooks)

---

**Версия**: 1.0  
**Статус**: 📊 Аналитический отчёт  
**Следующий шаг**: Обсудить приоритеты и начать с P0 (ks3Borders или EstimateWithSidebar)
