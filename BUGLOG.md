# BUGLOG - Журнал дефектов проекта Smetalab v6

## Информация об окружении

- **Дата начала тестирования**: 2026-01-11
- **Версия проекта**: smetalab v6
- **Frontend URL**: http://localhost:3000
- **Backend URL**: http://localhost:3001/api
- **Тестировщик**: Antigravity AI

---

## Статистика багов

| Статус | Количество |
|--------|------------|
| Open | 0 |
| Fixed | 5 |
| Won't Fix | 0 |
| Can't Reproduce | 0 |
| **Всего** | **5** |

---

## Список дефектов

---

### BUG-001: NullPointerException при логине super_admin без tenant

| Поле | Значение |
|------|----------|
| **ID** | BUG-001 |
| **Серьёзность** | critical |
| **Модуль/экран** | server/controllers/authController.js, login() |
| **Статус** | Fixed ✅ |

**Шаги воспроизведения:**
1. Создать пользователя super_admin без привязки к tenant
2. Попытаться залогиниться через /api/auth/login

**Фактический результат:**
```
TypeError: Cannot read properties of null (reading 'id')
```
На строке 666: `id: selectedTenant.id` — selectedTenant может быть null для super_admin.

**Ожидаемый результат:**
Логин super_admin должен работать корректно, возвращая `tenant: null` или пустой объект.

**Логи/скрин/трейс:**
Строки 665-681 в authController.js — обращение к selectedTenant без проверки на null.

**Повторяемость:** 100% (для super_admin без tenant)

**Предполагаемая причина:**
На строках 545-560 selectedTenant может быть установлен в null для super_admin без tenants, но на строках 665-681 происходит безусловное обращение к свойствам selectedTenant без проверки.

---

### BUG-002: Unit тесты запускают миграции БД (globalSetup неправильно настроен)

| Поле | Значение |
|------|----------|
| **ID** | BUG-002 |
| **Серьёзность** | major |
| **Модуль/экран** | vitest.config.mjs, tests/integration/setup.integration.js |
| **Статус** | Fixed ✅ |

**Шаги воспроизведения:**
1. Запустить `npm run test:unit`
2. Наблюдать вывод в консоли

**Фактический результат:**
```
🔧 SETUP: Применение миграций для интеграционных тестов
❌ Ошибка в 003_create_works_table.sql: function current_tenant_id() does not exist
❌ Ошибка в 003_setup_rls.sql: permission denied to create role
```
Unit тесты пытаются запустить миграции БД, которые:
- Требуют подключения к реальной БД
- Падают с ошибками RLS функций
- Занимают много времени

**Ожидаемый результат:**
Unit тесты должны работать БЕЗ БД, используя только моки. `globalSetup` с миграциями должен применяться ТОЛЬКО к integration тестам.

**Логи/скрин/трейс:**
`vitest.config.mjs` строка 14: `globalSetup: ['./tests/integration/setup.integration.js']` — применяется ко всем тестам вместо только integration.

**Повторяемость:** 100%

**Предполагаемая причина:**
В `vitest.config.mjs` параметр `globalSetup` применяется глобально ко всем тестам. Нужно либо убрать его, либо использовать отдельные конфигурации для unit и integration тестов.

**Предлагаемое решение:**
1. Создать отдельные конфиги: `vitest.unit.config.mjs` и `vitest.integration.config.mjs`
2. Либо использовать workspace configuration в vitest
3. Либо убрать globalSetup и вызывать миграции вручную перед integration тестами

---

### BUG-003: Устаревшие unit тесты после рефакторинга UI компонентов

| Поле | Значение |
|------|----------|
| **ID** | BUG-003 |
| **Серьёзность** | major |
| **Модуль/экран** | tests/unit/estimates/components/*.test.jsx |
| **Статус** | Fixed ✅ |

**Шаги воспроизведения:**
1. Запустить `npm run test:unit`
2. Наблюдать 14 упавших тестов

**Фактический результат:**
```
Test Files: 2 failed | 14 passed (16)
Tests: 14 failed | 252 passed (266)
```
Тесты ищут устаревшие тексты и элементы, которых больше нет в компонентах:
- `MaterialsDialog.test.jsx`: ожидает `'Загрузка материалов...'`, компонент показывает `'Список пуст'`
- `MaterialsDialog.test.jsx`: ожидает подсказку `'Поиск в базе 47,000 материалов'`, изменено на `'🔍 Используйте пробел...'`
- Virtuoso виртуализация не рендерит все элементы сразу

**Ожидаемый результат:**
Все unit тесты должны проходить.

**Повторяемость:** 100%

**Предполагаемая причина:**
Компоненты были обновлены/переписаны, но unit тесты не были синхронизированы.

---

### BUG-004: Integration тесты падают из-за jsdom environment и OpenAI SDK

| Поле | Значение |
|------|----------|
| **ID** | BUG-004 |
| **Серьёзность** | major |
| **Модуль/экран** | vitest.config.mjs, tests/integration/*.test.js |
| **Статус** | Fixed ✅ |

**Шаги воспроизведения:**
1. Запустить `npm run test:integration`
2. Наблюдать ошибку

**Фактический результат:**
```
Error: It looks like you're running in a browser-like environment.
This is disabled by default, as it risks exposing your secret API credentials to attackers.
new OpenAI({ apiKey, dangerouslyAllowBrowser: true });
```
Все 10 integration test files падают сразу при импорте.

**Ожидаемый результат:**
Integration тесты должны использовать `environment: 'node'`, а не `jsdom`.

**Логи/скрин/трейс:**
- `server/services/semanticSearchService.js:8` — OpenAI SDK инициализируется
- vitest.config.mjs использует `environment: 'jsdom'` глобально

**Повторяемость:** 100%

**Предполагаемая причина:**
Vitest конфигурация указывает `environment: 'jsdom'` для всех тестов. Integration тесты тестируют Node.js API и НЕ должны использовать jsdom.

**Предлагаемое решение:**
1. Создать vitest workspace configuration
2. Либо указать `environment: 'node'` per-file через комментарий `// @vitest-environment node`

---

### BUG-005: Несоответствия API responses в integration тестах

| Поле | Значение |
|------|----------|
| **ID** | BUG-005 |
| **Серьёзность** | major |
| **Модуль/экран** | projects.api, users.api, API controllers |
| **Статус** | Fixed ✅ |

**Шаги воспроизведения:**
1. Запустить `npm run test:integration`
2. Наблюдать 4 упавших теста

**Фактический результат:**
```
Tests: 4 failed | 128 passed | 8 skipped (140)
```
Конкретные несоответствия:
1. `PUT /api/projects/:id` с `status: 'invalid_status'` → возвращает 500 вместо 400
2. `GET /api/projects/dashboard-summary` → `chartDataMonth` undefined
3. `POST /api/users` для слабого пароля → возвращает 409 вместо 400

**Ожидаемый результат:**
API должен возвращать корректные HTTP коды и данные согласно тестам.

**Повторяемость:** 100%

**Предполагаемая причина:**
1. Возможно API не валидирует status перед сохранением → 500 из PostgreSQL
2. Dashboard API может был изменен и больше не возвращает `chartDataMonth`
3. Валидация пароля может выполняться после проверки дубликата email → 409 вместо 400

**Требует дополнительного анализа:**
- Проверить контроллеры на правильную валидацию
- Сверить тесты с актуальной документацией API

---

<!-- Баги будут добавляться ниже в формате:

### BUG-XXX: Заголовок бага

| Поле | Значение |
|------|----------|
| **ID** | BUG-XXX |
| **Серьёзность** | blocker/critical/major/minor |
| **Модуль/экран** | |
| **Статус** | Open |

**Шаги воспроизведения:**
1. 
2. 
3. 

**Фактический результат:**

**Ожидаемый результат:**

**Логи/скрин/трейс:**

**Повторяемость:** 100% / иногда / 1 раз

**Предполагаемая причина:**

---
-->

