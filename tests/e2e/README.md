# E2E Тесты - Smeta Application

## 📋 Обзор

Этот каталог содержит комплексный набор End-to-End (E2E) тестов для приложения Smeta. Тесты написаны с использованием [Playwright](https://playwright.dev/) и покрывают все основные функциональности системы.

## 📊 Статистика покрытия

| Метрика | Значение |
|---------|----------|
| **Всего тестов** | 180+ |
| **Модулей покрыто** | 19 из 21 (90%) |
| **Время выполнения** | ~7 минут |
| **Браузер** | Chromium |

## 🏗️ Структура тестов

```
tests/e2e/
├── fixtures/           # Вспомогательные функции
│   └── authHelpers.js  # Хелперы для аутентификации
├── account/            # Тесты настроек аккаунта
│   ├── account-settings.spec.js
│   └── profile.spec.js
├── admin/              # Тесты админ-панели
│   ├── admin.spec.js
│   ├── permissions.spec.js
│   └── roles.spec.js
├── auth/               # Тесты аутентификации
│   ├── login.spec.js
│   ├── logout.spec.js
│   ├── register.spec.js
│   └── password-reset.spec.js
├── contracts/          # Тесты договоров
│   └── contracts.spec.js
├── dashboard/          # Тесты дашборда
│   └── dashboard.spec.js
├── estimate-templates/ # Тесты шаблонов смет
│   └── estimate-templates.spec.js
├── estimates/          # Тесты смет
│   ├── create-estimate.spec.js
│   ├── estimates-full.spec.js
│   ├── excel-export.spec.js
│   └── object-parameters.spec.js
├── landing/            # Тесты лендинга
│   └── landing.spec.js
├── projects/           # Тесты проектов
│   └── create-project.spec.js
├── purchases/          # Тесты закупок
│   └── purchases.spec.js
├── references/         # Тесты справочников
│   ├── counterparties.spec.js
│   ├── materials.spec.js
│   ├── work-materials.spec.js
│   └── works.spec.js
├── schedules/          # Тесты графиков работ
│   └── schedules.spec.js
└── work-completion/    # Тесты выполненных работ
    └── work-completion.spec.js
```

## 🚀 Запуск тестов

### Предварительные требования

1. **Node.js** версии 18+ 
2. **Запущенный frontend** на `http://localhost:3000`
3. **Запущенный backend** на `http://localhost:3001`
4. **Подключение к базе данных** (PostgreSQL на Render)

### Команды запуска

```bash
# Установка зависимостей Playwright
npx playwright install

# Запуск всех тестов
npx playwright test

# Запуск с подробным выводом
npx playwright test --reporter=line

# Запуск конкретного файла
npx playwright test tests/e2e/auth/login.spec.js

# Запуск с отладкой (пошаговый режим)
npx playwright test --debug

# Запуск с UI режимом
npx playwright test --ui

# Запуск в headed режиме (видно браузер)
npx playwright test --headed

# Запуск конкретного теста по имени
npx playwright test -g "should successfully login"
```

### Генерация отчёта

```bash
# Открыть HTML отчёт после выполнения тестов
npx playwright show-report tests/e2e/reports
```

## 🔧 Конфигурация

Конфигурация находится в файле `playwright.config.js`:

| Параметр | Значение | Описание |
|----------|----------|----------|
| `timeout` | 60000ms | Таймаут одного теста |
| `expect.timeout` | 10000ms | Таймаут ожидания |
| `fullyParallel` | true | Параллельный запуск |
| `retries` | 0 (2 на CI) | Повторы при ошибках |
| `baseURL` | http://localhost:3000 | Базовый URL фронтенда |

## 📝 Вспомогательные функции

### authHelpers.js

Файл `fixtures/authHelpers.js` содержит функции для работы с аутентификацией:

```javascript
// Создание тестового пользователя
const user = await createTestUser(page, {
  email: 'custom@example.com',
  password: 'Password123!',
  fullName: 'Иван Иванов',
  companyName: 'ООО Тест'
});

// Создание и логин пользователя (одна функция)
const user = await createAndLoginUser(page, context);

// Логин через UI
await loginViaUI(page, email, password);

// Выход из системы
await logoutViaUI(page);

// Проверка авторизации
await expectToBeAuthenticated(page);
await expectToBeUnauthenticated(page);
```

## 🧪 Покрытые модули

### ✅ Аутентификация (auth/)
- Регистрация нового пользователя
- Вход в систему
- Выход из системы
- Восстановление пароля
- Валидация полей форм

### ✅ Дашборд (dashboard/)
- Отображение статистики
- Виджеты доходов и расходов
- Навигация по меню
- Карточки проектов

### ✅ Проекты (projects/)
- Создание проекта
- Просмотр списка проектов
- Открытие деталей проекта
- Валидация формы

### ✅ Сметы (estimates/)
- Шаблоны смет
- Создание сметы
- Добавление работ
- Экспорт в Excel
- Параметры объекта

### ✅ Справочники (references/)
- Работы
- Материалы
- Контрагенты
- Связь работ с материалами

### ✅ Закупки (purchases/)
- Глобальный список закупок
- Фильтрация и поиск
- Создание закупки

### ✅ Договоры (contracts/)
- Генерация из сметы
- Скачивание DOCX
- Статусы договоров

### ✅ Графики работ (schedules/)
- Диаграмма Ганта
- Даты и продолжительность
- Прогресс выполнения

### ✅ Выполненные работы (work-completion/)
- Акты КС-2
- Акты КС-3
- Экспорт в Excel
- Расчёт итогов

### ✅ Администрирование (admin/)
- Управление пользователями
- Роли и права
- Матрица разрешений

### ✅ Аккаунт (account/)
- Настройки профиля
- Смена пароля
- Уведомления

### ✅ Лендинг (landing/)
- Главная страница
- Навигация
- Формы обратной связи

### ⏭️ Не покрыто (по согласованию)
- `email.js` — требует реального SMTP сервера
- `tenants.js` — управление тенантами (опционально)

## 🐛 Решение проблем

### Ошибка "duplicate key value violates unique constraint"

Эта ошибка возникает при параллельном запуске тестов. Решение: в `authHelpers.js` используется уникальный идентификатор `${timestamp}-${randomId}` для каждого тестового пользователя.

### Ошибка "Connection timeout"

База данных на Render может иметь задержки при холодном старте. Решения:
1. Повторите запуск тестов
2. Откройте приложение в браузере для прогрева соединения
3. Увеличьте таймаут в конфигурации

### Ошибка "Test timeout exceeded"

Увеличьте таймаут в `playwright.config.js`:
```javascript
timeout: 60000, // 60 секунд
```

### Элемент не найден

Проверьте:
1. Запущен ли frontend (`npm run dev`)
2. Запущен ли backend (`npm run server`)
3. Корректность селекторов в тестах

## 📈 Best Practices

1. **Изоляция тестов** — каждый тест создаёт нового пользователя
2. **Уникальные данные** — timestamp + randomId для избежания конфликтов
3. **Ожидание загрузки** — используйте `waitForLoadState('networkidle')`
4. **Гибкие селекторы** — комбинируйте несколько селекторов через `|`
5. **Таймауты** — устанавливайте разумные таймауты для каждой операции

## 🔗 Полезные ссылки

- [Playwright Documentation](https://playwright.dev/docs/intro)
- [Playwright API Reference](https://playwright.dev/docs/api/class-playwright)
- [Best Practices](https://playwright.dev/docs/best-practices)
- [Test Generator](https://playwright.dev/docs/codegen)

## 👥 Контакты

При возникновении вопросов по тестам обращайтесь к команде разработки.

---

*Последнее обновление: Декабрь 2025*
